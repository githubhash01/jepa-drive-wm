"""Single SimpleFeaturePyramid dense probe over frozen V-JEPA 2.1 final features.

One architecture, two tasks (``--task depth | semseg``) that differ only in ground truth,
output head, loss, and metric. See ``train.py`` / ``config.py`` / ``tasks.py``.
"""
